/*
 * Heater.h
 *
 *  Created on: Jan 25, 2015
 *      Author: micha
 */

#ifndef HEATER_H
#define HEATER_H

#include <string>
#include <list>
#include <iostream>

#include "Base.h"

typedef enum HeaterState
{
   HeaterOn,
   HeaterOff
}HeaterState;

class Room;

class Heater: public Base {
public:
      Heater(unsigned int _Id, std::string _Name, const Room* _pRoom);
      ~Heater(){}

      // access functions
      HeaterState  getState() const {return State;}
      HeaterState  toggleState();

      // construction functions
protected:
      Heater(): Base(0, ""), _State(LightOff), _pRoom(nullptr) {
         std::cout << "Wrong default constructor Light";
      }
      HeaterState  State;
      Room*        pRoom;
};

#endif /* HEATER_H */
