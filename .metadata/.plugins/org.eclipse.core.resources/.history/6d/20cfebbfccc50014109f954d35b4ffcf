/*
 * Base.h
 *
 *  Created on: Feb 12, 2015
 *      Author: micha
 */

#ifndef SRC_BASE_H_
#define SRC_BASE_H_

#include <iostream>

class Base {
   public:
      // Constructors
      Base(unsigned int _Id, std::string _Name) :
            _Name(_Name), _Id(_Id) {
      }
      Base(const Base &base) :  // copy constructor
            _Name(base.getName()), _Id(base.getId(),
                  _State(base.getState(), _pRoom(base.getRoom()))) {
      }  // copy constructor
      virtual ~Base() {
      }

      // Operators
      Base& operator=(Base other) {
         _Name = other.getName();
         _Id = other.getId();
         _State = other.getState();
         _pRoom = other.getRoom();
         return *this;
      }

      // access functions
      virtual std::string getName() const {
         return _Name;
      }
      virtual unsigned int getId() const {
         return _Id;
      }
      virtual unsigned int getState() const {
         return _State;
      }
      virtual unsigned int getRoom() const {
         return _pRoom;
      }

   protected:
      Base() :
            _Name(""), _Id(0), _State(false), _pRoom(nullptr) {
         std::cout << "Wrong default constructor Base";
      }
      std::string _Name;
      unsigned int _Id;
      bool _State;
      Room* _pRoom;
};

#endif /* SRC_BASE_H_ */
