/*
 * XmlParser.h
 *
 *  Created on: Jan 25, 2015
 *      Author: micha
 */

#ifndef SRC_XMLPARSER_H_
#define SRC_XMLPARSER_H_

#include <boost/property_tree/xml_parser.hpp>
#include <boost/property_tree/ptree.hpp>
#include <boost/date_time/gregorian/gregorian.hpp>
#include <string>
#include <list>

typedef boost::gregorian::date Date;

struct ptree
{
   std::string                                data;     // data associated with the node
   std::list< std::pair<std::string, ptree> > children; // ordered list of named children
};

struct Flight
{
    std::string  carrier;
    unsigned     number;
    Date         date;
    bool         cancelled;
};

typedef std::vector<Flight> Skedule;

Skedule readxml( const std::string &filename );

#endif /* SRC_XMLPARSER_H_ */
